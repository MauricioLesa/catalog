spring:
  datasource:
    url: jdbc:postgresql://postgresdb:5432/catalog
    username: catalog_user
    password: catalog_4432
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
      database: postgresql
      database-plataform: org.hibernate.dialect.PostgreSQLDialect
